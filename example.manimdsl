fun bubbleSort(arr: Array<number>) {
    let n = arr.size();
    for (i in range(0, n)) {
        for (j in range(0, n - i - 1)) {
            if (arr[j] > arr[j + 1]) {
                arr.swap(j, j+1);
            }
        }
    }
}


let root = Array<number>() {4, 2, 1};
bubbleSort(root);

