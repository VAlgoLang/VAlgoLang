fun factorial(number x, Stack<number> s): number {
    if(x == 0) {
        return 1;
    }
    let y = x * factorial(x - 1, s);
    s.push(y);
    return y;
}

let stack = new Stack<number>;
let x = factorial(3, stack);
